# --- Secrets: NEVER commit. .env carries live DSNs + the Anthropic API key. ---
.env
*.env.local

# --- Python ---
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Runtime / generated data (image store, JSON persistence stores) ---
data/
logs/

# --- Local operator notes (list login accounts / share password; keep out of VCS) ---
LOGIN_ACCESS.txt
SHARE_ACCESS.txt

# --- Local editor / harness state ---
.claude/settings.local.json

# --- OS noise ---
Thumbs.db
.DS_Store
